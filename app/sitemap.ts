import type {MetadataRoute} from 'next';
export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://prernafoundation.org';return ['','/about-us','/projects','/impact','/programs','/gallery','/contact','/donate-now','/credits','/education','/environment','/health','/natural-farming','/skill-development','/water-conservation','/women-empowerment'].map(url=>({url:`${base}${url}`,lastModified:new Date()}))}
