import {Header,Footer} from '@/components/SiteChrome';import {publicContent} from '@/lib/content';import Link from 'next/link';export default async function Projects(){const {projects}=await publicContent();return <><Header/><main><section className="page-hero"><div className="shell"><span className="eyebrow">Our work</span><h1>Projects with purpose</h1></div></section><section className="shell section"><div className="grid">{projects.map((p:any)=><article className="card project-card" key={p.slug}><span className="tag">{p.status}</span><h2>{p.name}</h2><p>{p.shortDescription}</p><Link href={`/${p.slug}`}>Explore project →</Link></article>)}</div></section></main><Footer/></>}
