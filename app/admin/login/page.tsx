'use client'; import { useState } from 'react'; export default function Login() { const [error, setError] = useState(''); async function go(e: any) { e.preventDefault(); const r = await fetch('/api/admin/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(Object.fromEntries(new FormData(e.currentTarget))) }); if (r.ok) location.href = '/admin'; else setError('Invalid email or password.') } return <main className="admin"><form className="login card form" onSubmit={go}><span className="eyebrow">Prerna Foundation</span><h1>Admin sign in</h1><label>Email<input name="email" type="email" required /></label><label>Password<input name="password" type="password" required /></label><button className="btn">Sign in</button>{error && <p className="notice">{error}</p>}<p>Use the seed command to create the first super admin.</p></form></main> }
