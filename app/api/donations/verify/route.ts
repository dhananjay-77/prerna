import {NextResponse} from 'next/server';import crypto from 'crypto';import {connectDB} from '@/lib/db';import {Donation} from '@/models';import nodemailer from 'nodemailer';
export async function POST(r:Request){try{const {razorpay_order_id,razorpay_payment_id,razorpay_signature,donationId}=await r.json();if(!process.env.RAZORPAY_KEY_SECRET||![razorpay_order_id,razorpay_payment_id,razorpay_signature,donationId].every((v)=>typeof v==='string'))return NextResponse.json({error:'Invalid payment verification data'},{status:400});const expected=crypto.createHmac('sha256',process.env.RAZORPAY_KEY_SECRET).update(`${razorpay_order_id}|${razorpay_payment_id}`).digest('hex');const supplied=Buffer.from(razorpay_signature);const calculated=Buffer.from(expected);if(supplied.length!==calculated.length||!crypto.timingSafeEqual(calculated,supplied))return NextResponse.json({error:'Invalid signature'},{status:400});await connectDB();const existing=await Donation.findById(donationId);if(!existing||existing.orderId!==razorpay_order_id)return NextResponse.json({error:'Donation not found'},{status:404});if(existing.status==='successful')return NextResponse.json({receiptNumber:existing.receiptNumber,emailStatus:existing.emailStatus||'not_configured'});const receipt=`PF-${new Date().getFullYear()}-${String(Date.now()).slice(-7)}`;const d=await Donation.findByIdAndUpdate(donationId,{status:'successful',paymentId:razorpay_payment_id,receiptNumber:receipt},{new:true});if(!d)return NextResponse.json({error:'Donation not found'},{status:404});let emailStatus='not_configured';if(process.env.SMTP_HOST&&process.env.FROM_EMAIL){const t=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:process.env.SMTP_SECURE==='true',auth:process.env.SMTP_USER&&process.env.SMTP_PASSWORD?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASSWORD}:undefined});try{await t.sendMail({from:process.env.FROM_EMAIL,to:d.email,subject:'Thank You for Supporting Prerna Foundation',text:`Dear ${d.name},\n\nThank you for your donation of ₹${d.amount} towards ${d.purpose}.\nReceipt: ${receipt}\nPayment reference: ${razorpay_payment_id}\n\nPrerna Foundation`});emailStatus='sent'}catch{console.error('Donation receipt email failed.');emailStatus='failed'}}d.emailStatus=emailStatus;await d.save();return NextResponse.json({receiptNumber:receipt,emailStatus})}catch{console.error('Donation verification failed.');return NextResponse.json({error:'Verification could not be completed.'},{status:400})}}
