'use client';import {useEffect,useState} from 'react';export default function Donations(){const [items,setItems]=useState<any[]>([]),[error,setError]=useState('');useEffect(()=>{fetch('/api/admin/donations').then(async r=>r.ok?setItems(await r.json()):setError('Only a Super Admin can view financial records.'))},[]);return <main className="admin"><header className="admin-head"><div className="shell"><a href="/admin">← Admin dashboard</a></div></header><section className="shell section"><span className="eyebrow">Financial records</span><h1>Donations</h1>{error&&<p className="notice">{error}</p>}<table className="data-table"><thead><tr><th>Receipt</th><th>Donor</th><th>Amount</th><th>Purpose</th><th>Status</th><th>Date</th></tr></thead><tbody>{items.map(x=><tr key={x._id}><td>{x.receiptNumber||'—'}</td><td>{x.name}<br/><small>{x.email}</small></td><td>₹{x.amount}</td><td>{x.purpose}</td><td>{x.status}</td><td>{new Date(x.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table>{!items.length&&!error&&<p>No donations recorded.</p>}</section></main>}
